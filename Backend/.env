DB_NAME=proyecto_rayo
DB_USER=rayo_app
DB_PASS=1q2w3e4r
DB_HOST=localhost
DB_PORT=5432
DB_DIALECT=postgres

//Si es 1 aplicamos los cambios en las tablas con respecto al RLS.
APPLY_RLS = 0

DB_RESET = 0

# En tu entorno
 DB_SYNC = 0
 DB_SYNC_ALTER = 0

# reiniciá el backend
# Configuración del servidor
PORT=3000
NODE_ENV=development

# Configuración de JWT
JWT_SECRET=tu_clave_secreta_muy_segura_aqui
SECRET_KEY=tu_clave_secreta_muy_segura_aqui

# Configuración de CORS (opcional)
CORS_ORIGIN=http://localhost:3001 

# Configuración de Email (opcional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

# URL del frontend (para armar links en emails)
FRONTEND_URL=http://localhost:3001

# Expiración del token de recuperación de contraseña (minutos)
RESET_TOKEN_EXP_MINUTES=15 